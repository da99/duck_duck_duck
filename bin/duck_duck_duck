#!/usr/bin/env ruby
#

case ARGV[0]

when 'help'
  puts "========================"
  puts ""
  puts "help"
  puts "up"
  puts "down"
  puts "migrate_schema"
  puts ""
  puts "=== ENV/options: ==="
  puts ""
  puts "DUCK_DUCK_DUCK_schema=_schema_"
  puts ""
  puts "========================"

else
  require 'duck_duck_duck'
  cmds = %w{ up down migrate_schema }
  fail "Unknown cmd: #{ARGV[0]}" unless cmds.include?(ARGV[0])
  Duck_Duck_Duck.send(ARGV)

end



